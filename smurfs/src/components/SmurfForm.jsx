import React from 'react';
import { Field, Form, Formik } from 'formik';

export default function SmurfForm(props) {

    return (
        <div>
            <Formik
                initialValues
                onSubmit
                render={() => {
                    
                }}
            />
        </div>
    )
}